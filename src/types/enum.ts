export enum MessageTypeEnum {
  // internal
  Connection = 'connection',
  Disconnect = 'disconnect',

  All = 'all',
  Leave = 'leave',
  Offer = 'offer',
  Answer = 'answer',
  Candidate = 'candidate',
}
